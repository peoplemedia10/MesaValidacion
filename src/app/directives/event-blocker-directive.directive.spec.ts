import { EventBlockerDirectiveDirective } from './event-blocker-directive.directive';

describe('EventBlockerDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EventBlockerDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
